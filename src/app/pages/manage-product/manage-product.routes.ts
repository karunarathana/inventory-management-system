import { Routes } from '@angular/router';
import { ManageProductComponent } from './manage-product.component';

export const MANAGE_PRODUCT_ROUTES: Routes = [
  { path: '', component: ManageProductComponent },
];